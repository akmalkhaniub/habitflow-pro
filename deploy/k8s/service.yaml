apiVersion: v1
kind: Service
metadata:
  name: habitflow-service
  labels:
    app: habitflow-pro
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    app: habitflow-pro
