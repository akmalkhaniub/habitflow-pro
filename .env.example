# RevenueCat public SDK keys (from the RevenueCat dashboard → API keys).
# Set these in app.json under expo.extra instead for a build, or wire via EAS secrets.
# When unset, the app runs a simulated store so the purchase UX is fully demoable.
REVENUECAT_IOS_KEY=
REVENUECAT_ANDROID_KEY=
